.DS_Store
node_modules/
dist/
.vercel/
.env
.env.local
*.log
*.tsbuildinfo

# Mac installer is hosted externally (S3) — too large for GitHub
public/downloads/**/*.dmg

# Local Netlify folder
.netlify

# Local agent/tooling scratch — never part of the deployed site
.claude/
.playwright-cli/
output/

# Visual-regression screenshots. Local build artefacts, not source: they were
# tracked by mistake and added ~200 PNGs to the repo.
verification/
